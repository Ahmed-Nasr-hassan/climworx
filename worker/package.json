{
  "name": "icon-api-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=dist/index.js --format=esm --target=es2022 --minify",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
